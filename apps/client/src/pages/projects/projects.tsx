import { Stack, Typography } from "@mui/material";

export const ProjectsPage = () => {
  return (
    <Stack sx={{ width: "100%", alignItems: "center" }}>
      <Typography variant="h3">Welcome to projects page!</Typography>
    </Stack>
  );
};
